p1 = (X)--[e: l]->(Y)                   // Graph matching, First pattern

     ↪
       del X                                             // Removing a node

       (X);                                              // Instance of reference to be returned

// ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~

p2 = (X) --[e: x]->(Y)
         --[f: y]->(Z)
        ;
        ↪  del Z

        (X)
